kafka.topic.name=transactions-topic
logging.level.org.springframework.web=INFO

# Local dev without Docker: activate dev profile (H2, no Redis/Kafka required)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
